include ../includes/workshop-item.pug

doctype html
html(lang="de")
    head
        meta(charset="utf-8")
        meta(name="viewport" content="width=device-width, initial-scale=1.0")
        meta(name="robots" content="noindex")
        link(rel="stylesheet" href="/resource/index.css")
        style.
            body {
                margin: -10px;
            }

    body
        #wrapper(style="margin-top: 0;")
            .container#shows
                .workshop-list
                    each workshop in workshops
                        +workshop-item(workshop, "_blank")
