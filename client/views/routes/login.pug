extends ../layout.pug

block routeContent
    h1 LOGIN

    form#login-form
        label(for="login") Benutzername / Email
        input.login-field(type="text" name="login")
        label(for="password") Passwort
        input(type="password" name="password")

        input(type="submit" value="Anmelden")
